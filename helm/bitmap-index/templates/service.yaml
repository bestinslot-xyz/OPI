apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitmap-index.fullname" . }}
  labels:
    {{- include "bitmap-index.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitmap-index.selectorLabels" . | nindent 4 }}
