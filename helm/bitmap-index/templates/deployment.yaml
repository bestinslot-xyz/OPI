apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bitmap-index.fullname" . }}
  labels:
    {{- include "bitmap-index.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bitmap-index.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "bitmap-index.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bitmap-index.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
          - name: DB_USER
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.user
          - name: DB_HOST
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.host
          - name: DB_DATABASE
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.database
          - name: DB_PASSWD
            valueFrom:
              secretKeyRef:
                name: metadb-app
                key: password
          - name: DB_METAPROTOCOL_USER
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.user
          - name: DB_METAPROTOCOL_HOST
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.host
          - name: DB_METAPROTOCOL_PORT
            value: "5432"
          - name: DB_METAPROTOCOL_DATABASE
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: metaDb.database
          - name: DB_METAPROTOCOL_PASSWD
            valueFrom:
              secretKeyRef:
                name: metadb-app
                key: password
          - name: NETWORK_TYPE
            valueFrom:
              configMapKeyRef:
                name: bis-config
                key: networkType
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
