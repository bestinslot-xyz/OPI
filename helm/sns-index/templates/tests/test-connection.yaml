apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sns-index.fullname" . }}-test-connection"
  labels:
    {{- include "sns-index.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sns-index.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
