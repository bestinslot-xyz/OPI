apiVersion: v1
kind: Service
metadata:
  name: {{ include "sns-index.fullname" . }}
  labels:
    {{- include "sns-index.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sns-index.selectorLabels" . | nindent 4 }}
