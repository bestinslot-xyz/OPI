apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runes-index.fullname" . }}-test-connection"
  labels:
    {{- include "runes-index.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runes-index.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
