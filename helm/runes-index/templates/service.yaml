apiVersion: v1
kind: Service
metadata:
  name: {{ include "runes-index.fullname" . }}
  labels:
    {{- include "runes-index.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runes-index.selectorLabels" . | nindent 4 }}
