apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "main-index.fullname" . }}-test-connection"
  labels:
    {{- include "main-index.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "main-index.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
