apiVersion: v1
kind: Service
metadata:
  name: {{ include "pow20-api.fullname" . }}
  labels:
    {{- include "pow20-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pow20-api.selectorLabels" . | nindent 4 }}
