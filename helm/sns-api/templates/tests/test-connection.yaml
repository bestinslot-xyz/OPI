apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sns-api.fullname" . }}-test-connection"
  labels:
    {{- include "sns-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sns-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
