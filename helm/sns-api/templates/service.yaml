apiVersion: v1
kind: Service
metadata:
  name: {{ include "sns-api.fullname" . }}
  labels:
    {{- include "sns-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sns-api.selectorLabels" . | nindent 4 }}
