apiVersion: v1
kind: Service
metadata:
  name: {{ include "pow20-index.fullname" . }}
  labels:
    {{- include "pow20-index.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pow20-index.selectorLabels" . | nindent 4 }}
