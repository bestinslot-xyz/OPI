apiVersion: v1
kind: Service
metadata:
  name: {{ include "runes-api.fullname" . }}
  labels:
    {{- include "runes-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runes-api.selectorLabels" . | nindent 4 }}
